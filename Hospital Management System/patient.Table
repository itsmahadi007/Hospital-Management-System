CREATE TABLE [dbo].[Table]
(
	[Id] INT NOT NULL PRIMARY KEY, 
    [patient_name] NCHAR(10) NOT NULL, 
    [age] VARCHAR(50) NOT NULL, 
    [address] VARCHAR(50) NOT NULL, 
    [phone] VARCHAR(50) NOT NULL, 
    CONSTRAINT [PK_Table] PRIMARY KEY ([Id])
)
